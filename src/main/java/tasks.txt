- Add user event for snap lock

- float aspectRatio = 16.0f / 9.0f; Change from hardcoded values to a default. Allow user to change.

- Fix the bugs

- Make sure every class is commented correctly.

- Reload to git with a working Main branch and a Dev branch
